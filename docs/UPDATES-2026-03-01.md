# Updates — 2026-03-01

## Branding / identity
- Renamed assistant identity from `moltbot69` to **DarLight Copilot** (presentable hackathon name).
- Workspace file updated: `/home/ubuntu/.openclaw/workspace/IDENTITY.md`.

## Postgres RAG knowledge base (VPS-only data)
- Implemented a Postgres-backed RAG/knowledge system where:
  - Repo contains **code + schema only**.
  - VPS contains **actual data** (encrypted) and backups.
  - Sensitive data/tokens are **not committed** to git.

### Repo additions (code-only)
- `rag/schema.sql` (pgcrypto enabled; encrypted fields for message bodies and summaries)
- `rag/scripts/`:
  - `init_tree.py` — initializes topic tree
  - `ingest_openclaw_sessions.py` — ingests OpenClaw session JSONL (Telegram + UI) into Postgres (encrypted + redacted)
  - `auto_tag.py` — rule-based auto-tagging; writes derived keywords + message-topic links
  - `show_branch.py` — displays a topic branch with decrypted snippets (VPS only)
  - `summarize_topics.py` — generates encrypted “fruitful summaries” into `rag_summary` + safe teasers
- Docs:
  - `docs/RAG-KNOWLEDGE.md`
  - `docs/SUBAGENTS-RAG.md` — how sub-agents share the same Postgres RAG brain

### VPS deployment details (do not publish)
- Postgres installed on VPS; bound to `localhost` (`listen_addresses=localhost`).
- DB/user created: `rag` / database `rag`.
- Encryption key stored only on VPS:
  - `/home/ubuntu/.openclaw/rag/enc_key`
  - env file: `/home/ubuntu/.openclaw/rag/rag.env`

### Backups
- Daily rotating local backups via systemd:
  - `/usr/local/bin/rag-pg-backup.sh`
  - `rag-pg-backup.timer` → dumps to `/home/ubuntu/.openclaw/backups/rag/` (keeps last 30)

### Current ingestion status (at time of implementation)
- OpenClaw sessions ingested (encrypted):
  - Inserted ~771 messages across two session JSONL files.
- Auto-tagging executed:
  - Linked messages: 779
  - Inserted keywords: 18,910
- Summaries generated:
  - summaries_updated: 7

## Notes / safety
- Do not paste or commit dashboard `#token=...` URLs.
- Do not paste or commit `OPENCLAW_GATEWAY_TOKEN`.

