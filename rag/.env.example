# Postgres connection
RAG_DATABASE_URL=postgresql://rag:rag@127.0.0.1:55432/rag

# Field-level encryption key (keep ONLY on VPS; do not commit)
# 32+ chars recommended
RAG_ENCRYPTION_KEY=CHANGE_ME_LONG_RANDOM

# Optional: set to 1 to refuse to run if key is default
RAG_REQUIRE_STRONG_KEY=1
